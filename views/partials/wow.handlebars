<section id="wow">
    <div class="container">
        <h2 class="section-title">WOW</h2>
        <div class="title-border"></div>
        {{> wow-svg}}
        <div class="wow-text">
            <p>{{ tl.wow.alinea1 }}</p>
            <p>{{ tl.wow.alinea2 }}</p>
            <p>{{ tl.wow.alinea3 }} <a href="/wow">{{ tl.wow.link }}</a></p>
            <a href="/wow" class="btn-primary-gradient">LAKRAN presents WOW</a>
            <a href="/who-is-daphne" class="btn-primary-invert">{{ tl.wow.whoisdaphne }}</a>
        </div>
    </div>
</section>